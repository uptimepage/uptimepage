//! What every askama surface shares: the filter functions the derives call,
//! the fingerprinted static assets, and the timestamp and duration formats.

pub mod assets;
pub mod filters;
pub mod format;
